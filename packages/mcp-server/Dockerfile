FROM node:20-alpine
# TODO: This stub will be replaced by J-116 Docker infra.
# Adding ARGs here to fulfill J-120 requirements in the meantime.
ARG GIT_SHA
ARG BUILT_AT
ENV GIT_SHA=${GIT_SHA}
ENV BUILT_AT=${BUILT_AT}
