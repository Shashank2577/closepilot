{
  "name": "@closepilot/orchestrator",
  "version": "0.1.0",
  "private": true,
  "description": "Orchestrator agent for managing deal lifecycle and agent coordination",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "typecheck": "tsc --noEmit",
    "lint": "echo 'Linting not configured yet'",
    "test": "vitest run",
    "worker": "node --loader ts-node/esm src/worker.ts"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.32.0",
    "@closepilot/core": "workspace:*",
    "@closepilot/db": "workspace:*",
    "@closepilot/mcp-client": "workspace:*",
    "@closepilot/agents-ingestion": "workspace:*",
    "@closepilot/agent-enrichment": "workspace:*",
    "@closepilot/agent-scoping": "workspace:*",
    "proposal": "workspace:*",
    "@closepilot/agent-crm-sync": "workspace:*",
    "bullmq": "^5.75.2",
    "ioredis": "^5.10.1",
    "@opentelemetry/api": "^1.9.0"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
